# Marketing CRM - Stub

Estructura inicial de proyecto para el módulo de Marketing (monorepo).  
Incluye:

- `backend/` (Java Spring Boot, monolito modular)
- `frontend/` (React + TypeScript)
- `docs/` e `infra/` de ejemplo
